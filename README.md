# MailHush

Tiny Plasmo extension for replacing specific email addresses with custom phrases on any website while preserving the surrounding styling.

## Run

```bash
bun install
bun run dev
```

Load the unpacked extension from `build/chrome-mv3-dev`.

## Build

```bash
bun run build
```
